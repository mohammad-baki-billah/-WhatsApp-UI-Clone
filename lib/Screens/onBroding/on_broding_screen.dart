import 'package:flutter/material.dart';

class OnBrodingScreen extends StatelessWidget {
  const OnBrodingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('On Broding'),),
    );
  }
}